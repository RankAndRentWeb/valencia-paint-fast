---------------

import SiteLayout from '../layouts/SiteLayout.astro';

const {

const props = Astro.props;

  title,export interface Props {

const localBusinessSchema = {

  "@context": "https://schema.org",  subtitle, 

  "@type": "LocalBusiness",

  "name": "Pintores en Valencia",  price,  title: string;export interface Props {const { entry } = Astro.props;

  "url": "https://pintores-valencia.com",

  "telephone": "+34722208131",  priceNote,

  "email": "info@pintores-valencia.com"

};  badges,  subtitle: string;



const faqSchema = {  features,

  "@context": "https://schema.org",

  "@type": "FAQPage",  heroImg,  price: string;  title: string;const { Content } = await entry.render();

  "mainEntity": props.faqs.map(faq => ({

    "@type": "Question",  heroAlt,

    "name": faq.question,

    "acceptedAnswer": {  problem,  priceNote: string;

      "@type": "Answer",

      "text": faq.answer  solution,

    }

  }))  process,  badges: string[];  subtitle: string;const data = entry.data;

};

---  proseTitle,



<SiteLayout   proseP1,  features: string[];

  title={props.seoTitle}

  description={props.seoDescription}  proseP2,

  canonical={props.canonical}

  keywords={props.keywords}  proseList,  heroImg: string;  price: string;const hasImg = !!data?.heroImage;

  schema={[localBusinessSchema, faqSchema]}

  ogImage="https://pintores-valencia.com/og-home.webp"  faqs,

>

  <main>  seoTitle,  heroAlt: string;

    <!-- Hero Section -->

    <section class="bg-white py-16">  seoDescription,

      <div class="container mx-auto px-4">

        <div class="grid lg:grid-cols-2 gap-12 items-center">  canonical,  problem: string[];  priceNote: string;---

          <div>

            <h1 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-6">{props.title}</h1>  keywords

            <p class="text-xl text-gray-600 mb-8">{props.subtitle}</p>

            } = Astro.props;  solution: string[];

            <div class="flex flex-wrap gap-3 mb-8">

              {props.badges.map(badge => (

                <span class="bg-green-100 text-green-800 px-3 py-1 rounded-full text-sm font-medium">

                  ✓ {badge}const localBusinessSchema = {  process: { title: string; desc: string }[];  badges: string[];

                </span>

              ))}  "@context": "https://schema.org",

            </div>

              "@type": "LocalBusiness",  proseTitle: string;

            <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-8">

              <div class="text-3xl font-bold text-blue-600 mb-2">{props.price}</div>  "name": "Pintores en Valencia",

              <p class="text-gray-600">{props.priceNote}</p>

            </div>  "url": "https://pintores-valencia.com",  proseP1: string;  features: string[];<section class="container mx-auto px-4 py-10 grid lg:grid-cols-2 gap-10">

            

            <div class="flex flex-col sm:flex-row gap-4">  "telephone": "+34722208131",

              <a href="/presupuesto/" class="bg-orange-500 text-white px-8 py-4 rounded-lg text-lg font-bold hover:bg-orange-600 text-center">

                Presupuesto Gratis  "email": "info@pintores-valencia.com"  proseP2: string;

              </a>

              <a href="tel:+34722208131" class="bg-blue-600 text-white px-8 py-4 rounded-lg text-lg font-bold hover:bg-blue-700 text-center">};

                📞 Llamar Ahora

              </a>  proseList: string[];  heroImg: string;  <div>

            </div>

          </div>const faqSchema = {

          

          <div>  "@context": "https://schema.org",  faqs: { question: string; answer: string }[];

            <img src={props.heroImg} alt={props.heroAlt} class="w-full h-auto rounded-lg shadow-lg" loading="eager" />

          </div>  "@type": "FAQPage",

        </div>

      </div>  "mainEntity": faqs.map(faq => ({  seoTitle: string;  heroAlt: string;    {data.badges?.length && (

    </section>

    "@type": "Question",

    <!-- Features -->

    <section class="bg-gray-50 py-16">    "name": faq.question,  seoDescription: string;

      <div class="container mx-auto px-4">

        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">¿Por qué elegirnos?</h2>    "acceptedAnswer": {

        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">

          {props.features.map(feature => (      "@type": "Answer",  canonical: string;  problem: string[];      <div class="flex flex-wrap items-center gap-2 mb-4">

            <div class="text-center">

              <div class="bg-blue-100 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">      "text": faq.answer

                <span class="text-2xl">✓</span>

              </div>    }  keywords?: string;

              <h3 class="font-semibold text-gray-900 mb-2">{feature}</h3>

            </div>  }))

          ))}

        </div>};}  solution: string[];        {data.badges.map((b) => <span class="text-sm px-3 py-1 rounded-full bg-orange-50 text-orange-700 border border-orange-200">{b}</span>)}

      </div>

    </section>---



    <!-- Problem/Solution -->

    <section class="bg-white py-16">

      <div class="container mx-auto px-4"><!DOCTYPE html>

        <div class="grid lg:grid-cols-2 gap-12">

          <div><html lang="es">const props = Astro.props;  process: { title: string; desc: string }[];      </div>

            <h2 class="text-2xl font-bold text-gray-900 mb-6">❌ Problemas comunes</h2>

            <ul class="space-y-4"><head>

              {props.problem.map(item => (

                <li class="flex items-start">  <meta charset="UTF-8" />

                  <span class="text-red-500 mr-3 mt-1">✗</span>

                  <span class="text-gray-700">{item}</span>  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

                </li>

              ))}  <title>{seoTitle}</title>const localBusinessSchema = {  proseTitle: string;    )}

            </ul>

          </div>  <meta name="description" content={seoDescription} />

          <div>

            <h2 class="text-2xl font-bold text-gray-900 mb-6">✅ Nuestra solución</h2>  <link rel="canonical" href={canonical} />  "@context": "https://schema.org",

            <ul class="space-y-4">

              {props.solution.map(item => (  {keywords && <meta name="keywords" content={keywords} />}

                <li class="flex items-start">

                  <span class="text-green-500 mr-3 mt-1">✓</span>  <meta property="og:type" content="website" />  "@type": "LocalBusiness",  proseP1: string;    <h1 class="text-4xl md:text-5xl font-extrabold text-gray-800 mb-4">{data.title}</h1>

                  <span class="text-gray-700">{item}</span>

                </li>  <meta property="og:title" content={seoTitle} />

              ))}

            </ul>  <meta property="og:description" content={seoDescription} />  "name": "Pintores en Valencia",

          </div>

        </div>  <meta property="og:url" content={canonical} />

      </div>

    </section>  <meta property="og:image" content="https://pintores-valencia.com/og-home.webp" />  "url": "https://pintores-valencia.com",  proseP2: string;    {data.subtitle && <p class="text-lg text-gray-600 mb-4">{data.subtitle}</p>}



    <!-- Process -->  <meta name="twitter:card" content="summary_large_image" />

    <section class="bg-gray-50 py-16">

      <div class="container mx-auto px-4">  <meta name="twitter:title" content={seoTitle} />  "telephone": "+34722208131",

        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">Nuestro proceso</h2>

        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">  <meta name="twitter:description" content={seoDescription} />

          {props.process.map((step, index) => (

            <div class="text-center">  <meta name="twitter:image" content="https://pintores-valencia.com/og-home.webp" />  "email": "info@pintores-valencia.com"  proseList: string[];    {(data.priceFrom && data.priceUnit) && (

              <div class="bg-blue-600 text-white w-12 h-12 rounded-full flex items-center justify-center mx-auto mb-4 text-xl font-bold">

                {index + 1}  <script type="application/ld+json" set:html={JSON.stringify(localBusinessSchema)} />

              </div>

              <h3 class="font-semibold text-gray-900 mb-2">{step.title}</h3>  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />};

              <p class="text-gray-600 text-sm">{step.desc}</p>

            </div>  <script src="https://cdn.tailwindcss.com"></script>

          ))}

        </div></head>  faqs: { question: string; answer: string }[];      <p class="text-2xl font-bold text-blue-700 mb-6">

      </div>

    </section>



    <!-- Prose Content --><body class="bg-gray-50">const faqSchema = {

    <section class="bg-white py-16">

      <div class="container mx-auto px-4 max-w-4xl">  <header class="bg-white shadow-sm">

        <div class="prose max-w-none">

          <h2 class="text-2xl font-bold text-gray-900 mb-4">{props.proseTitle}</h2>    <div class="container mx-auto px-4 py-4">  "@context": "https://schema.org",  seoTitle: string;        desde {data.priceFrom}{data.priceUnit} <span class="text-sm text-gray-500 font-normal">Material incluido</span>

          <p class="text-gray-700 mb-4 leading-relaxed">{props.proseP1}</p>

          <p class="text-gray-700 mb-4 leading-relaxed">{props.proseP2}</p>      <div class="flex items-center justify-between">

          {props.proseList.length > 0 && (

            <ul class="list-disc pl-6 space-y-2">        <a href="/" class="text-2xl font-bold text-blue-600">Pintores Valencia</a>  "@type": "FAQPage",

              {props.proseList.map(item => (

                <li class="text-gray-700">{item}</li>        <a href="tel:+34722208131" class="bg-blue-600 text-white px-6 py-2 rounded-lg font-semibold hover:bg-blue-700">

              ))}

            </ul>          📞 722 208 131  "mainEntity": props.faqs.map(faq => ({  seoDescription: string;      </p>

          )}

        </div>        </a>

      </div>

    </section>      </div>    "@type": "Question",



    <!-- FAQ -->    </div>

    <section class="bg-gray-50 py-16">

      <div class="container mx-auto px-4 max-w-4xl">  </header>    "name": faq.question,  canonical: string;    )}

        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">Preguntas frecuentes</h2>

        <div class="space-y-6">

          {props.faqs.map(faq => (

            <details class="bg-white rounded-lg shadow-sm">  <main>    "acceptedAnswer": {

              <summary class="p-6 font-semibold text-gray-900 cursor-pointer hover:bg-gray-50">

                {faq.question}    <!-- Hero Section -->

              </summary>

              <div class="px-6 pb-6 text-gray-700">{faq.answer}</div>    <section class="bg-white py-16">      "@type": "Answer",  keywords?: string;    <div class="flex flex-wrap gap-3 mb-8">

            </details>

          ))}      <div class="container mx-auto px-4">

        </div>

      </div>        <div class="grid lg:grid-cols-2 gap-12 items-center">      "text": faq.answer

    </section>

          <div>

    <!-- Final CTA -->

    <section class="bg-blue-600 py-16">            <h1 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-6">{title}</h1>    }}      <a href="/presupuesto/" class="h-11 px-6 rounded-md bg-accent hover:bg-accent/90 text-white font-semibold shadow flex items-center">Pedir presupuesto gratis</a>

      <div class="container mx-auto px-4 text-center">

        <h2 class="text-3xl font-bold text-white mb-6">¿Listo para empezar?</h2>            <p class="text-xl text-gray-600 mb-8">{subtitle}</p>

        <p class="text-xl text-blue-100 mb-8">Solicita tu presupuesto gratuito sin compromiso</p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center">              }))

          <a href="/presupuesto/" class="bg-orange-500 text-white px-8 py-4 rounded-lg text-lg font-bold hover:bg-orange-600">

            Presupuesto Gratis            <div class="flex flex-wrap gap-3 mb-8">

          </a>

          <a href="tel:+34722208131" class="bg-white text-blue-600 px-8 py-4 rounded-lg text-lg font-bold hover:bg-gray-100">              {badges.map(badge => (};      <a href="tel:+34722208131" class="h-11 px-6 rounded-md bg-blue-600 hover:bg-blue-700 text-white font-semibold inline-flex items-center gap-2">

            📞 722 208 131

          </a>                <span class="bg-green-100 text-green-800 px-3 py-1 rounded-full text-sm font-medium">

        </div>

      </div>                  ✓ {badge}---

    </section>

  </main>                </span>

</SiteLayout>
              ))}const {        722 208 131

            </div>

            <!DOCTYPE html>

            <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-8">

              <div class="text-3xl font-bold text-blue-600 mb-2">{price}</div><html lang="es">  title,      </a>

              <p class="text-gray-600">{priceNote}</p>

            </div><head>

            

            <div class="flex flex-col sm:flex-row gap-4">  <meta charset="UTF-8" />  subtitle,    </div>

              <a href="/presupuesto/" class="bg-orange-500 text-white px-8 py-4 rounded-lg text-lg font-bold hover:bg-orange-600 text-center">

                Presupuesto Gratis  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

              </a>

              <a href="tel:+34722208131" class="bg-blue-600 text-white px-8 py-4 rounded-lg text-lg font-bold hover:bg-blue-700 text-center">  <title>{props.seoTitle}</title>  price,    {data.bullets?.length && (

                📞 Llamar Ahora

              </a>  <meta name="description" content={props.seoDescription} />

            </div>

          </div>  <link rel="canonical" href={props.canonical} />  priceNote,      <div class="grid grid-cols-3 gap-6 my-6">

          

          <div>  {props.keywords && <meta name="keywords" content={props.keywords} />}

            <img src={heroImg} alt={heroAlt} class="w-full h-auto rounded-lg shadow-lg" loading="eager" />

          </div>  <meta property="og:type" content="website" />  badges,        {data.bullets.map(b => (

        </div>

      </div>  <meta property="og:title" content={props.seoTitle} />

    </section>

  <meta property="og:description" content={props.seoDescription} />  features,          <div class="flex items-center gap-2 text-gray-700">

    <!-- Features -->

    <section class="bg-gray-50 py-16">  <meta property="og:url" content={props.canonical} />

      <div class="container mx-auto px-4">

        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">¿Por qué elegirnos?</h2>  <meta property="og:image" content="https://pintores-valencia.com/og-home.webp" />  heroImg,            <svg class="w-6 h-6 text-orange-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21.8 10a10 10 0 1 1-4.8-6.7"/><path d="m9 11 3 3L22 4"/></svg>

        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">

          {features.map(feature => (  <meta name="twitter:card" content="summary_large_image" />

            <div class="text-center">

              <div class="bg-blue-100 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">  <meta name="twitter:title" content={props.seoTitle} />  heroAlt,            <span>{b.label}</span>

                <span class="text-2xl">✓</span>

              </div>  <meta name="twitter:description" content={props.seoDescription} />

              <h3 class="font-semibold text-gray-900 mb-2">{feature}</h3>

            </div>  <meta name="twitter:image" content="https://pintores-valencia.com/og-home.webp" />  problem,          </div>

          ))}

        </div>  <script type="application/ld+json" set:html={JSON.stringify(localBusinessSchema)} />

      </div>

    </section>  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />  solution,        ))}



    <!-- Problem/Solution -->  <script src="https://cdn.tailwindcss.com"></script>

    <section class="bg-white py-16">

      <div class="container mx-auto px-4"></head>  process,      </div>

        <div class="grid lg:grid-cols-2 gap-12">

          <div>

            <h2 class="text-2xl font-bold text-gray-900 mb-6">❌ Problemas comunes</h2>

            <ul class="space-y-4"><body class="bg-gray-50">  proseTitle,    )}

              {problem.map(item => (

                <li class="flex items-start">  <header class="bg-white shadow-sm">

                  <span class="text-red-500 mr-3 mt-1">✗</span>

                  <span class="text-gray-700">{item}</span>    <div class="container mx-auto px-4 py-4">  proseP1,  </div>

                </li>

              ))}      <div class="flex items-center justify-between">

            </ul>

          </div>        <a href="/" class="text-2xl font-bold text-blue-600">Pintores Valencia</a>  proseP2,

          <div>

            <h2 class="text-2xl font-bold text-gray-900 mb-6">✅ Nuestra solución</h2>        <a href="tel:+34722208131" class="bg-blue-600 text-white px-6 py-2 rounded-lg font-semibold hover:bg-blue-700">

            <ul class="space-y-4">

              {solution.map(item => (          📞 722 208 131  proseList,  <div>

                <li class="flex items-start">

                  <span class="text-green-500 mr-3 mt-1">✓</span>        </a>

                  <span class="text-gray-700">{item}</span>

                </li>      </div>  faqs,    {hasImg && (

              ))}

            </ul>    </div>

          </div>

        </div>  </header>  seoTitle,      <img src={data.heroImage} width={data.heroWidth ?? 900} height={data.heroHeight ?? 600}

      </div>

    </section>



    <!-- Process -->  <main>  seoDescription,           alt={data.title} class="w-full rounded-2xl shadow-lg object-cover" loading="lazy" decoding="async" />

    <section class="bg-gray-50 py-16">

      <div class="container mx-auto px-4">    <!-- Hero Section -->

        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">Nuestro proceso</h2>

        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">    <section class="bg-white py-16">  canonical,    )}

          {process.map((step, index) => (

            <div class="text-center">      <div class="container mx-auto px-4">

              <div class="bg-blue-600 text-white w-12 h-12 rounded-full flex items-center justify-center mx-auto mb-4 text-xl font-bold">

                {index + 1}        <div class="grid lg:grid-cols-2 gap-12 items-center">  keywords  </div>

              </div>

              <h3 class="font-semibold text-gray-900 mb-2">{step.title}</h3>          <div>

              <p class="text-gray-600 text-sm">{step.desc}</p>

            </div>            <h1 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-6">{props.title}</h1>} = Astro.props;</section>

          ))}

        </div>            <p class="text-xl text-gray-600 mb-8">{props.subtitle}</p>

      </div>

    </section>            



    <!-- Prose Content -->            <div class="flex flex-wrap gap-3 mb-8">

    <section class="bg-white py-16">

      <div class="container mx-auto px-4 max-w-4xl">              {props.badges.map(badge => (const localBusinessSchema = {<section class="container mx-auto px-4 grid md:grid-cols-2 gap-6 mb-10">

        <div class="prose max-w-none">

          <h2 class="text-2xl font-bold text-gray-900 mb-4">{proseTitle}</h2>                <span class="bg-green-100 text-green-800 px-3 py-1 rounded-full text-sm font-medium">

          <p class="text-gray-700 mb-4 leading-relaxed">{proseP1}</p>

          <p class="text-gray-700 mb-4 leading-relaxed">{proseP2}</p>                  ✓ {badge}  "@context": "https://schema.org",  {data.problem && (

          {proseList.length > 0 && (

            <ul class="list-disc pl-6 space-y-2">                </span>

              {proseList.map(item => (

                <li class="text-gray-700">{item}</li>              ))}  "@type": "LocalBusiness",    <div class="rounded-2xl border border-red-200 bg-red-50 p-6">

              ))}

            </ul>            </div>

          )}

        </div>              "name": "Pintores en Valencia",      <h2 class="text-xl font-bold text-red-800 mb-3">{data.problem.title}</h2>

      </div>

    </section>            <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-8">



    <!-- FAQ -->              <div class="text-3xl font-bold text-blue-600 mb-2">{props.price}</div>  "url": "https://pintores-valencia.com",      <ul class="list-disc ml-5 text-red-900">{data.problem.items.map(i => <li>{i}</li>)}</ul>

    <section class="bg-gray-50 py-16">

      <div class="container mx-auto px-4 max-w-4xl">              <p class="text-gray-600">{props.priceNote}</p>

        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">Preguntas frecuentes</h2>

        <div class="space-y-6">            </div>  "telephone": "+34722208131",    </div>

          {faqs.map(faq => (

            <details class="bg-white rounded-lg shadow-sm">            

              <summary class="p-6 font-semibold text-gray-900 cursor-pointer hover:bg-gray-50">

                {faq.question}            <div class="flex flex-col sm:flex-row gap-4">  "email": "info@pintores-valencia.com"  )}

              </summary>

              <div class="px-6 pb-6 text-gray-700">{faq.answer}</div>              <a href="/presupuesto/" class="bg-orange-500 text-white px-8 py-4 rounded-lg text-lg font-bold hover:bg-orange-600 text-center">

            </details>

          ))}                Presupuesto Gratis};  {data.solution && (

        </div>

      </div>              </a>

    </section>

              <a href="tel:+34722208131" class="bg-blue-600 text-white px-8 py-4 rounded-lg text-lg font-bold hover:bg-blue-700 text-center">    <div class="rounded-2xl border border-green-200 bg-green-50 p-6">

    <!-- Final CTA -->

    <section class="bg-blue-600 py-16">                📞 Llamar Ahora

      <div class="container mx-auto px-4 text-center">

        <h2 class="text-3xl font-bold text-white mb-6">¿Listo para empezar?</h2>              </a>const faqSchema = {      <h2 class="text-xl font-bold text-green-800 mb-3">{data.solution.title}</h2>

        <p class="text-xl text-blue-100 mb-8">Solicita tu presupuesto gratuito sin compromiso</p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center">            </div>

          <a href="/presupuesto/" class="bg-orange-500 text-white px-8 py-4 rounded-lg text-lg font-bold hover:bg-orange-600">

            Presupuesto Gratis          </div>  "@context": "https://schema.org",      <ul class="list-disc ml-5 text-green-900">{data.solution.items.map(i => <li>{i}</li>)}</ul>

          </a>

          <a href="tel:+34722208131" class="bg-white text-blue-600 px-8 py-4 rounded-lg text-lg font-bold hover:bg-gray-100">          

            📞 722 208 131

          </a>          <div>  "@type": "FAQPage",    </div>

        </div>

      </div>            <img src={props.heroImg} alt={props.heroAlt} class="w-full h-auto rounded-lg shadow-lg" loading="eager" />

    </section>

  </main>          </div>  "mainEntity": faqs.map(faq => ({  )}



  <footer class="bg-gray-900 text-white py-8">        </div>

    <div class="container mx-auto px-4 text-center">

      <p>&copy; 2025 Pintores en Valencia. Todos los derechos reservados.</p>      </div>    "@type": "Question",</section>

      <div class="flex justify-center gap-6 mt-4">

        <a href="/aviso-legal/" class="text-gray-300 hover:text-white">Aviso Legal</a>    </section>

        <a href="/politica-privacidad/" class="text-gray-300 hover:text-white">Política de Privacidad</a>

        <a href="/politica-cookies/" class="text-gray-300 hover:text-white">Política de Cookies</a>    "name": faq.question,

      </div>

    </div>    <!-- Features -->

  </footer>

</body>    <section class="bg-gray-50 py-16">    "acceptedAnswer": {{data.process?.length && (

</html>
      <div class="container mx-auto px-4">

        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">¿Por qué elegirnos?</h2>      "@type": "Answer",<section class="container mx-auto px-4 mb-12">

        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">

          {props.features.map(feature => (      "text": faq.answer  <h2 class="text-2xl font-bold mb-4">Nuestro proceso</h2>

            <div class="text-center">

              <div class="bg-blue-100 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">    }  <ol class="grid md:grid-cols-2 gap-3 list-decimal ml-6 text-gray-700">{data.process.map(p => <li>{p}</li>)}</ol>

                <span class="text-2xl">✓</span>

              </div>  }))</section>

              <h3 class="font-semibold text-gray-900 mb-2">{feature}</h3>

            </div>};)}

          ))}

        </div>---

      </div>

    </section><section class="container mx-auto px-4 prose lg:prose-lg max-w-none mb-12">



    <!-- Problem/Solution --><!DOCTYPE html>  <Content />

    <section class="bg-white py-16">

      <div class="container mx-auto px-4"><html lang="es"></section>

        <div class="grid lg:grid-cols-2 gap-12">

          <div><head>

            <h2 class="text-2xl font-bold text-gray-900 mb-6">❌ Problemas comunes</h2>

            <ul class="space-y-4">  <meta charset="UTF-8" />{data.faq?.length && (

              {props.problem.map(item => (

                <li class="flex items-start">  <meta name="viewport" content="width=device-width, initial-scale=1.0" /><section class="container mx-auto px-4 mb-16">

                  <span class="text-red-500 mr-3 mt-1">✗</span>

                  <span class="text-gray-700">{item}</span>  <title>{seoTitle}</title>  <h2 class="text-2xl font-bold mb-4">Preguntas frecuentes</h2>

                </li>

              ))}  <meta name="description" content={seoDescription} />  <div class="divide-y border rounded-xl">

            </ul>

          </div>  <link rel="canonical" href={canonical} />    {data.faq.map(f => (

          <div>

            <h2 class="text-2xl font-bold text-gray-900 mb-6">✅ Nuestra solución</h2>  {keywords && <meta name="keywords" content={keywords} />}      <details class="p-4">

            <ul class="space-y-4">

              {props.solution.map(item => (  <meta property="og:type" content="website" />        <summary class="cursor-pointer font-semibold">{f.q}</summary>

                <li class="flex items-start">

                  <span class="text-green-500 mr-3 mt-1">✓</span>  <meta property="og:title" content={seoTitle} />        <p class="mt-2 text-gray-600">{f.a}</p>

                  <span class="text-gray-700">{item}</span>

                </li>  <meta property="og:description" content={seoDescription} />      </details>

              ))}

            </ul>  <meta property="og:url" content={canonical} />    ))}

          </div>

        </div>  <meta property="og:image" content="https://pintores-valencia.com/og-home.webp" />  </div>

      </div>

    </section>  <meta name="twitter:card" content="summary_large_image" /></section>



    <!-- Process -->  <meta name="twitter:title" content={seoTitle} />)}

    <section class="bg-gray-50 py-16">  <meta name="twitter:description" content={seoDescription} />

      <div class="container mx-auto px-4">  <meta name="twitter:image" content="https://pintores-valencia.com/og-home.webp" />

        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">Nuestro proceso</h2>  <script type="application/ld+json" set:html={JSON.stringify(localBusinessSchema)} />

        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />

          {props.process.map((step, index) => (  <script src="https://cdn.tailwindcss.com"></script>

            <div class="text-center"></head>

              <div class="bg-blue-600 text-white w-12 h-12 rounded-full flex items-center justify-center mx-auto mb-4 text-xl font-bold">

                {index + 1}<body class="bg-gray-50">

              </div>  <header class="bg-white shadow-sm">

              <h3 class="font-semibold text-gray-900 mb-2">{step.title}</h3>    <div class="container mx-auto px-4 py-4">

              <p class="text-gray-600 text-sm">{step.desc}</p>      <div class="flex items-center justify-between">

            </div>        <a href="/" class="text-2xl font-bold text-blue-600">Pintores Valencia</a>

          ))}        <a href="tel:+34722208131" class="bg-blue-600 text-white px-6 py-2 rounded-lg font-semibold hover:bg-blue-700">

        </div>          📞 722 208 131

      </div>        </a>

    </section>      </div>

    </div>

    <!-- Prose Content -->  </header>

    <section class="bg-white py-16">

      <div class="container mx-auto px-4 max-w-4xl">  <main>

        <div class="prose max-w-none">    <!-- Hero Section -->

          <h2 class="text-2xl font-bold text-gray-900 mb-4">{props.proseTitle}</h2>    <section class="bg-white py-16">

          <p class="text-gray-700 mb-4 leading-relaxed">{props.proseP1}</p>      <div class="container mx-auto px-4">

          <p class="text-gray-700 mb-4 leading-relaxed">{props.proseP2}</p>        <div class="grid lg:grid-cols-2 gap-12 items-center">

          {props.proseList.length > 0 && (          <div>

            <ul class="list-disc pl-6 space-y-2">            <h1 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-6">{title}</h1>

              {props.proseList.map(item => (            <p class="text-xl text-gray-600 mb-8">{subtitle}</p>

                <li class="text-gray-700">{item}</li>            

              ))}            <div class="flex flex-wrap gap-3 mb-8">

            </ul>              {badges.map(badge => (

          )}                <span class="bg-green-100 text-green-800 px-3 py-1 rounded-full text-sm font-medium">

        </div>                  ✓ {badge}

      </div>                </span>

    </section>              ))}

            </div>

    <!-- FAQ -->            

    <section class="bg-gray-50 py-16">            <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-8">

      <div class="container mx-auto px-4 max-w-4xl">              <div class="text-3xl font-bold text-blue-600 mb-2">{price}</div>

        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">Preguntas frecuentes</h2>              <p class="text-gray-600">{priceNote}</p>

        <div class="space-y-6">            </div>

          {props.faqs.map(faq => (            

            <details class="bg-white rounded-lg shadow-sm">            <div class="flex flex-col sm:flex-row gap-4">

              <summary class="p-6 font-semibold text-gray-900 cursor-pointer hover:bg-gray-50">              <a href="/presupuesto/" class="bg-orange-500 text-white px-8 py-4 rounded-lg text-lg font-bold hover:bg-orange-600 text-center">

                {faq.question}                Presupuesto Gratis

              </summary>              </a>

              <div class="px-6 pb-6 text-gray-700">{faq.answer}</div>              <a href="tel:+34722208131" class="bg-blue-600 text-white px-8 py-4 rounded-lg text-lg font-bold hover:bg-blue-700 text-center">

            </details>                📞 Llamar Ahora

          ))}              </a>

        </div>            </div>

      </div>          </div>

    </section>          

          <div>

    <!-- Final CTA -->            <img src={heroImg} alt={heroAlt} class="w-full h-auto rounded-lg shadow-lg" loading="eager" />

    <section class="bg-blue-600 py-16">          </div>

      <div class="container mx-auto px-4 text-center">        </div>

        <h2 class="text-3xl font-bold text-white mb-6">¿Listo para empezar?</h2>      </div>

        <p class="text-xl text-blue-100 mb-8">Solicita tu presupuesto gratuito sin compromiso</p>    </section>

        <div class="flex flex-col sm:flex-row gap-4 justify-center">

          <a href="/presupuesto/" class="bg-orange-500 text-white px-8 py-4 rounded-lg text-lg font-bold hover:bg-orange-600">    <!-- Features -->

            Presupuesto Gratis    <section class="bg-gray-50 py-16">

          </a>      <div class="container mx-auto px-4">

          <a href="tel:+34722208131" class="bg-white text-blue-600 px-8 py-4 rounded-lg text-lg font-bold hover:bg-gray-100">        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">¿Por qué elegirnos?</h2>

            📞 722 208 131        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">

          </a>          {features.map(feature => (

        </div>            <div class="text-center">

      </div>              <div class="bg-blue-100 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">

    </section>                <span class="text-2xl">✓</span>

  </main>              </div>

              <h3 class="font-semibold text-gray-900 mb-2">{feature}</h3>

  <footer class="bg-gray-900 text-white py-8">            </div>

    <div class="container mx-auto px-4 text-center">          ))}

      <p>&copy; 2025 Pintores en Valencia. Todos los derechos reservados.</p>        </div>

      <div class="flex justify-center gap-6 mt-4">      </div>

        <a href="/aviso-legal/" class="text-gray-300 hover:text-white">Aviso Legal</a>    </section>

        <a href="/politica-privacidad/" class="text-gray-300 hover:text-white">Política de Privacidad</a>

        <a href="/politica-cookies/" class="text-gray-300 hover:text-white">Política de Cookies</a>    <!-- Problem/Solution -->

      </div>    <section class="bg-white py-16">

    </div>      <div class="container mx-auto px-4">

  </footer>        <div class="grid lg:grid-cols-2 gap-12">

</body>          <div>

</html>            <h2 class="text-2xl font-bold text-gray-900 mb-6">❌ Problemas comunes</h2>
            <ul class="space-y-4">
              {problem.map(item => (
                <li class="flex items-start">
                  <span class="text-red-500 mr-3 mt-1">✗</span>
                  <span class="text-gray-700">{item}</span>
                </li>
              ))}
            </ul>
          </div>
          <div>
            <h2 class="text-2xl font-bold text-gray-900 mb-6">✅ Nuestra solución</h2>
            <ul class="space-y-4">
              {solution.map(item => (
                <li class="flex items-start">
                  <span class="text-green-500 mr-3 mt-1">✓</span>
                  <span class="text-gray-700">{item}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- Process -->
    <section class="bg-gray-50 py-16">
      <div class="container mx-auto px-4">
        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">Nuestro proceso</h2>
        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
          {process.map((step, index) => (
            <div class="text-center">
              <div class="bg-blue-600 text-white w-12 h-12 rounded-full flex items-center justify-center mx-auto mb-4 text-xl font-bold">
                {index + 1}
              </div>
              <h3 class="font-semibold text-gray-900 mb-2">{step.title}</h3>
              <p class="text-gray-600 text-sm">{step.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Prose Content -->
    <section class="bg-white py-16">
      <div class="container mx-auto px-4 max-w-4xl">
        <div class="prose max-w-none">
          <h2 class="text-2xl font-bold text-gray-900 mb-4">{proseTitle}</h2>
          <p class="text-gray-700 mb-4 leading-relaxed">{proseP1}</p>
          <p class="text-gray-700 mb-4 leading-relaxed">{proseP2}</p>
          {proseList.length > 0 && (
            <ul class="list-disc pl-6 space-y-2">
              {proseList.map(item => (
                <li class="text-gray-700">{item}</li>
              ))}
            </ul>
          )}
        </div>
      </div>
    </section>

    <!-- FAQ -->
    <section class="bg-gray-50 py-16">
      <div class="container mx-auto px-4 max-w-4xl">
        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">Preguntas frecuentes</h2>
        <div class="space-y-6">
          {faqs.map(faq => (
            <details class="bg-white rounded-lg shadow-sm">
              <summary class="p-6 font-semibold text-gray-900 cursor-pointer hover:bg-gray-50">
                {faq.question}
              </summary>
              <div class="px-6 pb-6 text-gray-700">{faq.answer}</div>
            </details>
          ))}
        </div>
      </div>
    </section>

    <!-- Final CTA -->
    <section class="bg-blue-600 py-16">
      <div class="container mx-auto px-4 text-center">
        <h2 class="text-3xl font-bold text-white mb-6">¿Listo para empezar?</h2>
        <p class="text-xl text-blue-100 mb-8">Solicita tu presupuesto gratuito sin compromiso</p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/presupuesto/" class="bg-orange-500 text-white px-8 py-4 rounded-lg text-lg font-bold hover:bg-orange-600">
            Presupuesto Gratis
          </a>
          <a href="tel:+34722208131" class="bg-white text-blue-600 px-8 py-4 rounded-lg text-lg font-bold hover:bg-gray-100">
            📞 722 208 131
          </a>
        </div>
      </div>
    </section>
  </main>

  <footer class="bg-gray-900 text-white py-8">
    <div class="container mx-auto px-4 text-center">
      <p>&copy; 2025 Pintores en Valencia. Todos los derechos reservados.</p>
      <div class="flex justify-center gap-6 mt-4">
        <a href="/aviso-legal/" class="text-gray-300 hover:text-white">Aviso Legal</a>
        <a href="/politica-privacidad/" class="text-gray-300 hover:text-white">Política de Privacidad</a>
        <a href="/politica-cookies/" class="text-gray-300 hover:text-white">Política de Cookies</a>
      </div>
    </div>
  </footer>
</body>
</html>